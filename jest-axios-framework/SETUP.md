# Покрокова інструкція встановлення та налаштування

Ця інструкція допоможе вам встановити та налаштувати API Automation Framework з нуля.

## Крок 1: Встановлення Node.js

### Перевірка встановлення

Спочатку перевірте, чи встановлений Node.js:

```bash
node --version
npm --version
```

Якщо команди не працюють, встановіть Node.js:

1. Завантажте Node.js з офіційного сайту: https://nodejs.org/
2. Встановіть LTS версію (рекомендовано)
3. Перевірте встановлення знову

**Очікуваний результат:**
```
v18.x.x  (або вище)
9.x.x    (або вище)
```

## Крок 2: Ініціалізація проекту

### Створення package.json

Якщо ви створюєте проект з нуля:

```bash
npm init -y
```

Це створить `package.json` з базовими налаштуваннями.

**Очікуваний результат:** Створено файл `package.json`

## Крок 3: Встановлення залежностей

### Встановлення основних пакетів

```bash
npm install axios dotenv
```

### Встановлення dev залежностей

```bash
npm install --save-dev jest
```

**Очікуваний результат:**
```
+ axios@1.6.0
+ dotenv@16.3.1
+ jest@29.7.0
```

### Встановлення репортерів (опціонально)

**Примітка:** Налаштування репортерів буде детально розглянуто на уроці. Тут наведено базову інструкцію.

Для генерації HTML та JUnit звітів встановіть репортери:

```bash
npm install --save-dev jest-junit jest-html-reporters
```

**Очікуваний результат:**
```
+ jest-junit@16.0.0
+ jest-html-reporters@3.1.5
```

**Що дають репортери:**
- **jest-html-reporters** - генерує красивий HTML звіт з результатами тестів
- **jest-junit** - генерує JUnit XML звіт для CI/CD (Jenkins, GitHub Actions)

**Де знаходяться звіти після запуску тестів:**
- `reports/html/report.html` - HTML звіт
- `reports/junit/junit.xml` - JUnit XML звіт
- `reports/coverage/` - звіти про покриття коду

**Запуск тестів з генерацією звітів:**
```bash
npm test
# або
npm run test:coverage
# або
npm run test:report
```

Всі команди генерують звіти автоматично, оскільки репортери налаштовані в `jest.config.js`.

## Крок 4: Створення структури папок

### Створення папок

```bash
mkdir -p src/services src/config src/utils src/data src/controllers
mkdir -p tests/smoke tests/functional tests/negative tests/integration tests/controllers tests/helpers tests/examples
```

**Структура має виглядати так:**
```
jest-axios-framework/
├── src/
│   ├── services/
│   ├── config/
│   ├── utils/
│   └── data/
├── tests/
│   ├── smoke/
│   ├── functional/
│   ├── negative/
│   ├── integration/
│   ├── controllers/
│   ├── helpers/
│   ├── examples/
│   ├── login.test.js
│   ├── token-demo-complete.test.js
│   ├── token-storage.json
│   └── token-demo-storage.json
├── reports/              # Звіти (створються автоматично після запуску тестів)
│   ├── coverage/        # Звіти про покриття коду
│   ├── html/            # HTML звіти
│   └── junit/           # JUnit XML звіти
├── package.json
└── jest.config.js
```

## Крок 5: Налаштування Jest

### Створення jest.config.js

Створіть файл `jest.config.js` в корені проекту:

```javascript
module.exports = {
  // Середовище для Node.js
  testEnvironment: 'node',
  
  // Патерн для пошуку тестових файлів
  testMatch: ['<rootDir>/tests/**/*.test.js'],
  
  // Timeout для тестів (10 секунд)
  testTimeout: 10000,
  
  // Детальний вивід
  verbose: true,
  
  // Coverage налаштування (якщо встановлені репортери)
  collectCoverage: true,
  collectCoverageFrom: [
    'src/**/*.js',
    '!src/**/index.js',
    '!**/node_modules/**',
  ],
  coverageDirectory: 'reports/coverage',
  coverageReporters: ['text', 'lcov', 'html'],
  
  // Репортери (якщо встановлені jest-junit та jest-html-reporters)
  reporters: [
    'default',
    [
      'jest-junit',
      {
        outputDirectory: 'reports/junit',
        outputName: 'junit.xml',
      },
    ],
    [
      'jest-html-reporters',
      {
        publicPath: 'reports/html',
        filename: 'report.html',
        expand: true,
        pageTitle: 'API Test Report (Jest)',
      },
    ],
  ],
};
```

**Примітка:** Якщо ви ще не встановили репортери (`jest-junit` та `jest-html-reporters`), можете використовувати спрощену версію без репортерів:

```javascript
module.exports = {
  testEnvironment: 'node',
  testMatch: ['<rootDir>/tests/**/*.test.js'],
  testTimeout: 10000,
  verbose: true
};
```

Репортери можна додати пізніше (детальніше на уроці).

**Очікуваний результат:** Створено файл `jest.config.js`

## Крок 6: Створення HTTP клієнта

### Створення src/services/httpClient.js

Створіть HTTP клієнт на базі Axios (див. готовий код у фреймворку).

**Очікуваний результат:** Створено файл `src/services/httpClient.js`

## Крок 7: Створення API сервісу

### Створення src/services/apiService.js

Створіть API сервіс (див. готовий код у фреймворку).

**Очікуваний результат:** Створено файл `src/services/apiService.js`

## Крок 8: Написання першого тесту

### Створення tests/smoke/smoke.test.js

Створіть простий smoke тест:

```javascript
const apiService = require('../../src/services/apiService');

describe('Smoke Tests', () => {
  test('API should be accessible', async () => {
    const response = await apiService.getAllUsers();
    expect(response.status).toBe(200);
  });
});
```

**Очікуваний результат:** Створено файл `tests/smoke/smoke.test.js`

## Крок 9: Запуск тестів

### Запуск всіх тестів

```bash
npm test
```

### Запуск конкретного тесту

```bash
npm test -- tests/smoke/smoke.test.js
```

### Запуск тестів у watch режимі

```bash
npm test -- --watch
```

### Запуск тестів з генерацією звітів

```bash
npm run test:coverage
# або
npm run test:report
```

**Очікуваний результат:**
```
PASS  tests/smoke/smoke.test.js
  Smoke Tests
    ✓ API should be accessible (1234 ms)

Test Suites: 1 passed, 1 total
Tests:       1 passed, 1 total
```

### Перегляд звітів

Після запуску тестів з репортерами, звіти будуть згенеровані в папці `reports/`:

- **HTML звіт**: Відкрийте `reports/html/report.html` в браузері
- **Coverage звіт**: Відкрийте `reports/coverage/index.html` в браузері  
- **JUnit XML**: `reports/junit/junit.xml` (для CI/CD)

**Примітка:** Детальне налаштування та робота з репортерами буде розглянуто на уроці.

## Крок 10: Налаштування environment variables (опціонально)

### Створення .env файлу

Створіть файл `.env` в корені проекту:

```env
BASE_URL=https://jsonplaceholder.typicode.com
REQUEST_TIMEOUT=10000
```

**Важливо:** Не комітьте `.env` файл в git! Додайте його в `.gitignore`.

## Перевірка встановлення

### Запуск всіх тестів

```bash
npm test
```

Якщо всі тести проходять успішно, встановлення завершено! 🎉

## Наступні кроки

1. Ознайомтеся з [README.md](./README.md) для деталей про фреймворк
2. Перегляньте приклади тестів у папці `tests/`
3. Створюйте свої тести на основі прикладів

## Troubleshooting

### Помилка: "Cannot find module 'axios'"

**Рішення:** Переконайтеся, що ви встановили залежності:
```bash
npm install
```

### Помилка: "Jest did not exit"

**Рішення:** Додайте `forceExit: true` в `jest.config.js`:
```javascript
module.exports = {
  // ...
  forceExit: true
};
```

### Помилка: "Request timeout"

**Рішення:** Збільште timeout в `jest.config.js`:
```javascript
module.exports = {
  // ...
  testTimeout: 30000 // 30 секунд
};
```

## Крок 11: Мокування даних (опціонально)

### Що таке мокування?

Мокування дозволяє замінити реальні HTTP запити на фейкові відповіді для тестування. Це корисно коли:
- API ще не готове
- Потрібно тестувати різні сценарії (помилки, таймаути)
- Потрібно швидко виконувати тести

### Базовий приклад мокування

Створіть тест з мокуванням `tests/mocking/example.test.js`:

```javascript
const axios = require('axios');

// Мокуємо axios модуль
jest.mock('axios');

describe('Mocked API Test', () => {
  test('should return mocked data', async () => {
    // Налаштовуємо мок
    axios.get.mockResolvedValue({
      status: 200,
      data: { id: 1, name: 'Mocked User' }
    });
    
    // Виконуємо запит
    const response = await axios.get('https://jsonplaceholder.typicode.com/users/1');
    
    // Перевіряємо результат
    expect(response.status).toBe(200);
    expect(response.data.name).toBe('Mocked User');
  });
});
```

### Запуск тестів з мокуванням

```bash
npm test -- tests/mocking
```

### Детальна документація

Більше інформації про мокування:
- [Документація про мокування](../../docs/04-mocking/README.md)
- [Практичні приклади](../../examples/mocking-examples.md)
- [Приклади тестів](tests/mocking/mocking.test.js)

---

**Готово!** Тепер ви можете почати писати свої API тести! 🚀

